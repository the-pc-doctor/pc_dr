# Identity denylist — EXAMPLE ONLY
#
# Copy to `.denylist.local` (gitignored) or point $PC_DR_DENYLIST at a file
# outside this repository, then replace every line below with your real values.
#
# NEVER COMMIT THE FILLED VERSION. A committed denylist of real names,
# hostnames, and addresses discloses exactly the values it exists to exclude,
# in the most convenient possible format. See SANITIZATION.md.
#
# One entry per line. Blank lines and `#` comments ignored.
# Entries are matched case-insensitively as whole words.
# A line starting with `re:` is treated as a regular expression.

# --- Personal and family names ---
Placeholder-Surname
Placeholder-Firstname
Placeholder-Familymember

# --- Usernames and account handles ---
placeholderuser
placeholder-admin

# --- Email addresses ---
placeholder@example.com
placeholder.alt@example.org

# --- Real hostnames, DDNS names, overlay names ---
placeholder-host.example-ddns.net
re:placeholder\d+\.ts\.net

# --- Device names that identify hardware or people ---
PLACEHOLDER_DEVICE_01
PLACEHOLDER-LAPTOP

# --- Device serials and vendor identifiers ---
re:PLACEHOLDER-[A-Z0-9]{8}

# --- Record identifiers (ticket numbers, document IDs) ---
re:#4[0-9]{5}\b

# --- Geographic coordinates, if any appear in configuration ---
re:\b-?\d{1,2}\.\d{4,}\s*,\s*-?\d{1,3}\.\d{4,}\b
