# Evidence

Bounded records of what actually happened when doctrine met production.

Nothing identifying belongs here. An incident becomes evidence only after the
device names, addresses, and record identifiers are removed — at which point it
is usually a doctrine revision instead. See `SYNC.md`.
