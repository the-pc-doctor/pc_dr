# Material change log

Records only material doctrine failures, contradictions, retrieval misses, scope or confidence changes, and architecture revisions. Ordinary successful use is not logged.

Each entry names: trigger and doctrine, expected versus observed behavior, decision effect, evidence, correction, and remaining uncertainty.

This log is evidence, not authority. When behavior should change, update the owning doctrine.

---

## 2026-07-29 — Repository established

**Trigger:** need for a publishable template covering the operational half of agent-run infrastructure, complementing an existing identity-and-judgment template.

**Decision effect:** governance, router, and publish-gate machinery adopted by design from `bobert-agent-template` (MIT, see `LINEAGE.md`); all doctrine, reference, stack, watchdog, and sanitization content authored independently.

**Remaining uncertainty:** the deny classes in `SANITIZATION.md` are a starting model, validated only against one lab's content. Expect additions the first time real material is projected.
